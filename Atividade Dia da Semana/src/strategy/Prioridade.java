package strategy;

/**
 * Representa o nível de prioridade que uma estratégia recomenda para
 * o dia. Usar enum em vez de String evita erro de digitação e deixa
 * explícito quais são os únicos valores possíveis.
 */
public enum Prioridade {
    ALTA,
    MEDIA,
    BAIXA
}
